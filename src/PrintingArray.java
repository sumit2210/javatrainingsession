/*public class PrintingArray {
    //1.	Find the sum of all elements in an array:
    //3.	Find the average of all elements in an array:
    //4.	Copy one array to another array:
    int[] oneDimensionalArray = {10,33,100,44,1};
    int len = oneDimensionalArray.length;

    // find minimum number in array
    if(oneDimensionalArray.length ==0){
        else{
            int minNumber = oneDimensionalArray[0];
        }
    }



}

 */
